#### 1. yarn 的使用

```
yarn init                    初始化一个新项目

yarn add 包名                 添加依赖包

yarn add 包名 --dev           添加到 devDependencies

yarn upgrade 包名             升级依赖包

yarn remove 包名              移除依赖包

yarn                          安装项目的全部依赖   (yarn install)
```
